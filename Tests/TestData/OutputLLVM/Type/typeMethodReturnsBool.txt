; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

%Number = type { i32 }

declare void @print(ptr %0)

define i1 @Number.isPositive(ptr nonnull %this) {
entry:
  %value = getelementptr inbounds nuw %Number, ptr %this, i32 0, i32 0
  %member_field_load = load i32, ptr %value, align 4
  %gttmp = icmp sgt i32 %member_field_load, 0
  ret i1 %gttmp
}

define i1 @func() {
entry:
  %n = alloca %Number, align 8
  call void @Number.new(ptr %n, i32 1)
  %0 = call i1 @Number.isPositive(ptr %n)
  ret i1 %0
}

define void @Number.new(ptr nonnull %this, i32 %value) {
entry:
  %value1 = getelementptr inbounds nuw %Number, ptr %this, i32 0, i32 0
  store i32 %value, ptr %value1, align 4
  ret void
}
