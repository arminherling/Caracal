; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define i32 @Zero._value(ptr nonnull %this) {
entry:
  ret i32 0
}

define void @Zero.new(ptr nonnull %this) {
entry:
  ret void
}
