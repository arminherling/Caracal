; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

%Box = type { i32 }

declare i32 @strcmp(ptr %0, ptr %1)

declare ptr @calloc(i64 %0, i64 %1)

declare ptr @realloc(ptr %0, i64 %1)

declare ptr @memmove(ptr %0, ptr %1, i64 %2)

define i32 @main() {
entry:
  %b = alloca %Box, align 8
  call void @Box.new(ptr %b, i32 40)
  %call = call i32 @Box.sum(ptr %b)
  ret i32 %call
}

define i32 @Box.sum(ptr nonnull %0) {
entry:
  %_value = getelementptr inbounds nuw %Box, ptr %0, i32 0, i32 0
  %load = load i32, ptr %_value, align 4
  %add = add i32 %load, 2
  ret i32 %add
}

define void @Box.new(ptr nonnull %0, i32 %1) {
entry:
  %_value = getelementptr inbounds nuw %Box, ptr %0, i32 0, i32 0
  store i32 %1, ptr %_value, align 4
  ret void
}
