; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define i32 @func() {
entry:
  br i1 true, label %if.true, label %if.continuation

if.true:                                          ; preds = %entry
  br label %if.continuation

if.continuation:                                  ; preds = %if.true, %entry
  %phi = phi i32 [ -1, %entry ], [ 0, %if.true ]
  ret i32 %phi
}
