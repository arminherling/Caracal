; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define void @swap(ptr nonnull %a, ptr nonnull %b) {
entry:
  %temp = alloca ptr, align 8
  store ptr %a, ptr %temp, align 8
  store ptr %b, ptr %a, align 8
  %temp1 = load ptr, ptr %temp, align 8
  store ptr %temp1, ptr %b, align 8
  ret void
}

declare void @print(ptr %0)
