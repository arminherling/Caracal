; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

%Number = type { i32 }

define i32 @func() {
entry:
  %ctor_tmp = alloca %Number, align 8
  call void @Number.new(ptr %ctor_tmp, i32 6)
  %ctor_result = load %Number, ptr %ctor_tmp, align 4
  %0 = call i32 @read(%Number %ctor_result)
  ret i32 %0
}

declare void @print(ptr %0)

define void @Number.new(ptr nonnull %this, i32 %value) {
entry:
  %value1 = getelementptr inbounds nuw %Number, ptr %this, i32 0, i32 0
  store i32 %value, ptr %value1, align 4
  ret void
}

define i32 @read(%Number %n) {
entry:
  %n.addr = alloca %Number, align 8
  store %Number %n, ptr %n.addr, align 4
  %value = getelementptr inbounds nuw %Number, ptr %n.addr, i32 0, i32 0
  %field_load = load i32, ptr %value, align 4
  ret i32 %field_load
}
