; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define i32 @func() {
entry:
  br label %while.condition

while.condition:                                  ; preds = %if.continuation2, %if.true, %entry
  %phi = phi i32 [ 0, %entry ], [ %add, %if.true ], [ %add, %if.continuation2 ]
  %phi3 = phi i32 [ 0, %entry ], [ %phi3, %if.true ], [ %add4, %if.continuation2 ]
  %less_than = icmp slt i32 %phi, 6
  br i1 %less_than, label %while.body, label %while.continuation

while.body:                                       ; preds = %while.condition
  %add = add i32 %phi, 1
  %equal = icmp eq i32 %add, 2
  br i1 %equal, label %if.true, label %if.continuation

if.true:                                          ; preds = %while.body
  br label %while.condition

if.continuation:                                  ; preds = %while.body
  %greater_or_equal = icmp sge i32 %add, 5
  br i1 %greater_or_equal, label %if.true1, label %if.continuation2

if.true1:                                         ; preds = %if.continuation
  br label %while.continuation

if.continuation2:                                 ; preds = %if.continuation
  %add4 = add i32 %phi3, %add
  br label %while.condition

while.continuation:                               ; preds = %if.true1, %while.condition
  ret i32 %phi3

; uselistorder directives
  uselistorder i32 %phi3, { 1, 2, 0 }
  uselistorder i32 %add, { 2, 3, 4, 1, 0 }
}
