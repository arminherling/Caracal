; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define void @inner(ptr nonnull %x) {
entry:
  ret void
}

declare void @print(ptr %0)

define void @outer() {
entry:
  %x = alloca i32, align 4
  store i32 20, ptr %x, align 4
  call void @inner(ptr %x)
  ret void
}
