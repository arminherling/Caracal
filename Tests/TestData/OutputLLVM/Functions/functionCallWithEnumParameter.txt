; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define void @inner(i8 %0) {
entry:
  ret void
}

define void @outer() {
entry:
  call void @inner(i8 0)
  ret void
}
