; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

define i32 @func() {
entry:
  %call = call i32 @Math.diff(i32 13, i32 10)
  ret i32 %call
}

define i32 @Math.diff(i32 %0, i32 %1) {
entry:
  %subtract = sub i32 %0, %1
  ret i32 %subtract
}

define void @Math.new(ptr nonnull %0) {
entry:
  ret void
}
