; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

%string = type { { ptr, i32, i32 } }

@0 = private unnamed_addr constant [6 x i8] c"Hello\00", align 1

declare i32 @strcmp(ptr %0, ptr %1)

declare ptr @calloc(i64 %0, i64 %1)

declare ptr @realloc(ptr %0, i64 %1)

declare ptr @memmove(ptr %0, ptr %1, i64 %2)

define i32 @sum({ ptr, i32 } %0) {
entry:
  %temp = alloca { ptr, i32 }, align 8
  br label %while.condition

while.condition:                                  ; preds = %while.body, %entry
  %phi = phi { ptr, i32 } [ %0, %entry ], [ %phi, %while.body ]
  %phi1 = phi i32 [ 0, %entry ], [ %add3, %while.body ]
  %phi2 = phi i32 [ 0, %entry ], [ %add, %while.body ]
  store { ptr, i32 } %phi, ptr %temp, align 8
  %length = getelementptr inbounds nuw { ptr, i32 }, ptr %temp, i32 0, i32 1
  %load = load i32, ptr %length, align 4
  %less_than = icmp slt i32 %phi1, %load
  br i1 %less_than, label %while.body, label %while.continuation

while.body:                                       ; preds = %while.condition
  %add = add i32 %phi2, 1
  %add3 = add i32 %phi1, 1
  br label %while.condition

while.continuation:                               ; preds = %while.condition
  ret i32 %phi2

; uselistorder directives
  uselistorder { ptr, i32 } %phi, { 1, 0 }
}

define i32 @double({ ptr, i32 } %0) {
entry:
  %call = call i32 @sum({ ptr, i32 } %0)
  %call1 = call i32 @sum({ ptr, i32 } %0)
  %add = add i32 %call, %call1
  ret i32 %add
}

define i32 @main() {
entry:
  %xs = alloca { ptr, i32, i32 }, align 8
  %s = alloca %string, align 8
  %call = call ptr @calloc(i64 8, i64 1)
  %data = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %xs, i32 0, i32 0
  store ptr %call, ptr %data, align 8
  %length = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %xs, i32 0, i32 1
  store i32 2, ptr %length, align 4
  %capacity = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %xs, i32 0, i32 2
  store i32 8, ptr %capacity, align 4
  %element = getelementptr i8, ptr %call, i32 0
  store i8 7, ptr %element, align 1
  %element5 = getelementptr i8, ptr %call, i32 1
  store i8 8, ptr %element5, align 1
  %_bytes = getelementptr inbounds nuw %string, ptr %s, i32 0, i32 0
  %data6 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 0
  store ptr @0, ptr %data6, align 8
  %length7 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 1
  store i32 5, ptr %length7, align 4
  %capacity8 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 2
  store i32 5, ptr %capacity8, align 4
  %data9 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %xs, i32 0, i32 0
  %load = load ptr, ptr %data9, align 8
  %length10 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %xs, i32 0, i32 1
  %load11 = load i32, ptr %length10, align 4
  %0 = insertvalue { ptr, i32 } poison, ptr %load, 0
  %slice = insertvalue { ptr, i32 } %0, i32 %load11, 1
  %call12 = call i32 @sum({ ptr, i32 } %slice)
  %_bytes13 = getelementptr inbounds nuw %string, ptr %s, i32 0, i32 0
  %data14 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes13, i32 0, i32 0
  %load15 = load ptr, ptr %data14, align 8
  %length16 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes13, i32 0, i32 1
  %load17 = load i32, ptr %length16, align 4
  %1 = insertvalue { ptr, i32 } poison, ptr %load15, 0
  %slice18 = insertvalue { ptr, i32 } %1, i32 %load17, 1
  %call19 = call i32 @sum({ ptr, i32 } %slice18)
  %_bytes20 = getelementptr inbounds nuw %string, ptr %s, i32 0, i32 0
  %data21 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes20, i32 0, i32 0
  %load22 = load ptr, ptr %data21, align 8
  %length23 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes20, i32 0, i32 1
  %load24 = load i32, ptr %length23, align 4
  %2 = insertvalue { ptr, i32 } poison, ptr %load22, 0
  %slice25 = insertvalue { ptr, i32 } %2, i32 %load24, 1
  %call26 = call i32 @double({ ptr, i32 } %slice25)
  %not_equal = icmp ne i32 %call12, 2
  br i1 %not_equal, label %if.true, label %if.continuation

if.true:                                          ; preds = %entry
  ret i32 2

if.continuation:                                  ; preds = %entry
  %not_equal27 = icmp ne i32 %call19, 5
  br i1 %not_equal27, label %if.true1, label %if.continuation2

if.true1:                                         ; preds = %if.continuation
  ret i32 3

if.continuation2:                                 ; preds = %if.continuation
  %not_equal28 = icmp ne i32 %call26, 10
  br i1 %not_equal28, label %if.true3, label %if.continuation4

if.true3:                                         ; preds = %if.continuation2
  ret i32 4

if.continuation4:                                 ; preds = %if.continuation2
  ret i32 42
}
