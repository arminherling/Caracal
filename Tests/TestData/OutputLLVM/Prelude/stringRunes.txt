; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

%string = type { { ptr, i32, i32 } }

@0 = private unnamed_addr constant [6 x i8] c"Hello\00", align 1
@1 = private unnamed_addr constant [13 x i8] c"gr\C3\BC\C3\9Fe \F0\9F\90\88\00", align 1
@2 = private unnamed_addr constant [4 x i8] c"\E2\82\AC\00", align 1

declare i32 @strcmp(ptr %0, ptr %1)

declare ptr @calloc(i64 %0, i64 %1)

declare ptr @realloc(ptr %0, i64 %1)

declare ptr @memmove(ptr %0, ptr %1, i64 %2)

define i32 @main() {
entry:
  %ascii = alloca %string, align 8
  %_bytes = getelementptr inbounds nuw %string, ptr %ascii, i32 0, i32 0
  %data = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 0
  store ptr @0, ptr %data, align 8
  %length = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 1
  store i32 5, ptr %length, align 4
  %capacity = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 2
  store i32 5, ptr %capacity, align 4
  %call = call i32 @string.runeCount(ptr %ascii)
  %not_equal = icmp ne i32 %call, 5
  %mixed = alloca %string, align 8
  %euro = alloca %string, align 8
  br i1 %not_equal, label %if.true, label %if.continuation

if.true:                                          ; preds = %entry
  ret i32 2

if.continuation:                                  ; preds = %entry
  %call25 = call i32 @string.runeAt(ptr %ascii, i32 0)
  %not_equal26 = icmp ne i32 %call25, 72
  br i1 %not_equal26, label %if.true1, label %if.continuation2

if.true1:                                         ; preds = %if.continuation
  ret i32 3

if.continuation2:                                 ; preds = %if.continuation
  %call27 = call i32 @string.runeLengthAt(ptr %ascii, i32 0)
  %not_equal28 = icmp ne i32 %call27, 1
  br i1 %not_equal28, label %if.true3, label %if.continuation4

if.true3:                                         ; preds = %if.continuation2
  ret i32 4

if.continuation4:                                 ; preds = %if.continuation2
  %_bytes29 = getelementptr inbounds nuw %string, ptr %mixed, i32 0, i32 0
  %data30 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes29, i32 0, i32 0
  store ptr @1, ptr %data30, align 8
  %length31 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes29, i32 0, i32 1
  store i32 12, ptr %length31, align 4
  %capacity32 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes29, i32 0, i32 2
  store i32 12, ptr %capacity32, align 4
  %call33 = call i32 @string.length(ptr %mixed)
  %not_equal34 = icmp ne i32 %call33, 12
  br i1 %not_equal34, label %if.true5, label %if.continuation6

if.true5:                                         ; preds = %if.continuation4
  ret i32 5

if.continuation6:                                 ; preds = %if.continuation4
  %call35 = call i32 @string.runeCount(ptr %mixed)
  %not_equal36 = icmp ne i32 %call35, 7
  br i1 %not_equal36, label %if.true7, label %if.continuation8

if.true7:                                         ; preds = %if.continuation6
  ret i32 6

if.continuation8:                                 ; preds = %if.continuation6
  %call37 = call i32 @string.runeLengthAt(ptr %mixed, i32 2)
  %not_equal38 = icmp ne i32 %call37, 2
  br i1 %not_equal38, label %if.true9, label %if.continuation10

if.true9:                                         ; preds = %if.continuation8
  ret i32 7

if.continuation10:                                ; preds = %if.continuation8
  %call39 = call i32 @string.runeAt(ptr %mixed, i32 2)
  %not_equal40 = icmp ne i32 %call39, 252
  br i1 %not_equal40, label %if.true11, label %if.continuation12

if.true11:                                        ; preds = %if.continuation10
  ret i32 8

if.continuation12:                                ; preds = %if.continuation10
  %call41 = call i32 @string.runeLengthAt(ptr %mixed, i32 8)
  %not_equal42 = icmp ne i32 %call41, 4
  br i1 %not_equal42, label %if.true13, label %if.continuation14

if.true13:                                        ; preds = %if.continuation12
  ret i32 9

if.continuation14:                                ; preds = %if.continuation12
  %call43 = call i32 @string.runeAt(ptr %mixed, i32 8)
  %not_equal44 = icmp ne i32 %call43, 128008
  br i1 %not_equal44, label %if.true15, label %if.continuation16

if.true15:                                        ; preds = %if.continuation14
  ret i32 10

if.continuation16:                                ; preds = %if.continuation14
  %_bytes45 = getelementptr inbounds nuw %string, ptr %euro, i32 0, i32 0
  %data46 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes45, i32 0, i32 0
  store ptr @2, ptr %data46, align 8
  %length47 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes45, i32 0, i32 1
  store i32 3, ptr %length47, align 4
  %capacity48 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes45, i32 0, i32 2
  store i32 3, ptr %capacity48, align 4
  %call49 = call i32 @string.length(ptr %euro)
  %not_equal50 = icmp ne i32 %call49, 3
  br i1 %not_equal50, label %if.true17, label %if.continuation18

if.true17:                                        ; preds = %if.continuation16
  ret i32 11

if.continuation18:                                ; preds = %if.continuation16
  %call51 = call i32 @string.runeCount(ptr %euro)
  %not_equal52 = icmp ne i32 %call51, 1
  br i1 %not_equal52, label %if.true19, label %if.continuation20

if.true19:                                        ; preds = %if.continuation18
  ret i32 12

if.continuation20:                                ; preds = %if.continuation18
  %call53 = call i32 @string.runeAt(ptr %euro, i32 0)
  %not_equal54 = icmp ne i32 %call53, 8364
  br i1 %not_equal54, label %if.true21, label %if.continuation22

if.true21:                                        ; preds = %if.continuation20
  ret i32 13

if.continuation22:                                ; preds = %if.continuation20
  br label %while.condition

while.condition:                                  ; preds = %while.body, %if.continuation22
  %phi = phi i32 [ 0, %if.continuation22 ], [ %add, %while.body ]
  %phi55 = phi i32 [ 0, %if.continuation22 ], [ %add58, %while.body ]
  %call56 = call i32 @string.length(ptr %mixed)
  %less_than = icmp slt i32 %phi, %call56
  br i1 %less_than, label %while.body, label %while.continuation

while.body:                                       ; preds = %while.condition
  %call57 = call i32 @string.runeLengthAt(ptr %mixed, i32 %phi)
  %add = add i32 %phi, %call57
  %add58 = add i32 %phi55, 1
  br label %while.condition

while.continuation:                               ; preds = %while.condition
  %not_equal59 = icmp ne i32 %phi55, 7
  br i1 %not_equal59, label %if.true23, label %if.continuation24

if.true23:                                        ; preds = %while.continuation
  ret i32 14

if.continuation24:                                ; preds = %while.continuation
  ret i32 42
}

define i32 @string.runeCount(ptr nonnull %0) {
entry:
  br label %while.condition

while.condition:                                  ; preds = %while.body, %entry
  %phi = phi i32 [ 0, %entry ], [ %add2, %while.body ]
  %phi1 = phi i32 [ 0, %entry ], [ %add, %while.body ]
  %_bytes = getelementptr inbounds nuw %string, ptr %0, i32 0, i32 0
  %length = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 1
  %load = load i32, ptr %length, align 4
  %less_than = icmp slt i32 %phi1, %load
  br i1 %less_than, label %while.body, label %while.continuation

while.body:                                       ; preds = %while.condition
  %call = call i32 @string.runeLengthAt(ptr %0, i32 %phi1)
  %add = add i32 %phi1, %call
  %add2 = add i32 %phi, 1
  br label %while.condition

while.continuation:                               ; preds = %while.condition
  ret i32 %phi
}

define i32 @string.runeAt(ptr nonnull %0, i32 %1) {
entry:
  %_bytes = getelementptr inbounds nuw %string, ptr %0, i32 0, i32 0
  %data = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 0
  %load = load ptr, ptr %data, align 8
  %element = getelementptr i8, ptr %load, i32 %1
  %load5 = load i8, ptr %element, align 1
  %int_widen = zext i8 %load5 to i32
  %call = call i32 @string.runeLengthAt(ptr %0, i32 %1)
  %equal = icmp eq i32 %call, 1
  br i1 %equal, label %if.true, label %if.continuation

if.true:                                          ; preds = %entry
  ret i32 %int_widen

if.continuation:                                  ; preds = %entry
  %equal6 = icmp eq i32 %call, 3
  br i1 %equal6, label %if.true1, label %if.continuation2

if.true1:                                         ; preds = %if.continuation
  br label %if.continuation2

if.continuation2:                                 ; preds = %if.true1, %if.continuation
  %phi = phi i32 [ 31, %if.continuation ], [ 15, %if.true1 ]
  %equal7 = icmp eq i32 %call, 4
  br i1 %equal7, label %if.true3, label %if.continuation4

if.true3:                                         ; preds = %if.continuation2
  br label %if.continuation4

if.continuation4:                                 ; preds = %if.true3, %if.continuation2
  %phi8 = phi i32 [ %phi, %if.continuation2 ], [ 7, %if.true3 ]
  %bit_and = and i32 %int_widen, %phi8
  br label %while.condition

while.condition:                                  ; preds = %while.body, %if.continuation4
  %phi9 = phi i32 [ %1, %if.continuation4 ], [ %phi9, %while.body ]
  %phi10 = phi i32 [ %int_widen, %if.continuation4 ], [ %phi10, %while.body ]
  %phi11 = phi i32 [ %call, %if.continuation4 ], [ %phi11, %while.body ]
  %phi12 = phi i32 [ %phi8, %if.continuation4 ], [ %phi12, %while.body ]
  %phi13 = phi i32 [ 1, %if.continuation4 ], [ %add22, %while.body ]
  %phi14 = phi i32 [ %bit_and, %if.continuation4 ], [ %bit_or, %while.body ]
  %less_than = icmp slt i32 %phi13, %phi11
  br i1 %less_than, label %while.body, label %while.continuation

while.body:                                       ; preds = %while.condition
  %_bytes15 = getelementptr inbounds nuw %string, ptr %0, i32 0, i32 0
  %add = add i32 %phi9, %phi13
  %data16 = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes15, i32 0, i32 0
  %load17 = load ptr, ptr %data16, align 8
  %element18 = getelementptr i8, ptr %load17, i32 %add
  %load19 = load i8, ptr %element18, align 1
  %int_widen20 = zext i8 %load19 to i32
  %shift_left = shl i32 %phi14, 6
  %bit_and21 = and i32 %int_widen20, 63
  %bit_or = or i32 %shift_left, %bit_and21
  %add22 = add i32 %phi13, 1
  br label %while.condition

while.continuation:                               ; preds = %while.condition
  ret i32 %phi14

; uselistorder directives
  uselistorder i32 %phi9, { 1, 0 }
  uselistorder i32 %phi11, { 1, 0 }
}

define i32 @string.runeLengthAt(ptr nonnull %0, i32 %1) {
entry:
  %_bytes = getelementptr inbounds nuw %string, ptr %0, i32 0, i32 0
  %data = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 0
  %load = load ptr, ptr %data, align 8
  %element = getelementptr i8, ptr %load, i32 %1
  %load5 = load i8, ptr %element, align 1
  %less_than = icmp ult i8 %load5, -128
  br i1 %less_than, label %if.true, label %if.continuation

if.true:                                          ; preds = %entry
  ret i32 1

if.continuation:                                  ; preds = %entry
  %less_than6 = icmp ult i8 %load5, -32
  br i1 %less_than6, label %if.true1, label %if.continuation2

if.true1:                                         ; preds = %if.continuation
  ret i32 2

if.continuation2:                                 ; preds = %if.continuation
  %less_than7 = icmp ult i8 %load5, -16
  br i1 %less_than7, label %if.true3, label %if.continuation4

if.true3:                                         ; preds = %if.continuation2
  ret i32 3

if.continuation4:                                 ; preds = %if.continuation2
  ret i32 4
}

define i32 @string.length(ptr nonnull %0) {
entry:
  %_bytes = getelementptr inbounds nuw %string, ptr %0, i32 0, i32 0
  %length = getelementptr inbounds nuw { ptr, i32, i32 }, ptr %_bytes, i32 0, i32 1
  %load = load i32, ptr %length, align 4
  ret i32 %load
}

; uselistorder directives
uselistorder ptr @string.runeCount, { 2, 1, 0 }
uselistorder ptr @string.runeAt, { 3, 2, 1, 0 }
uselistorder ptr @string.runeLengthAt, { 5, 4, 3, 2, 1, 0 }
uselistorder ptr @string.length, { 2, 1, 0 }
