; ModuleID = 'CaracalModule'
source_filename = "CaracalModule"
target triple = "x86_64-unknown-none"

declare i32 @strcmp(ptr %0, ptr %1)

declare ptr @calloc(i64 %0, i64 %1)

declare ptr @realloc(ptr %0, i64 %1)

declare ptr @memmove(ptr %0, ptr %1, i64 %2)

define i1 @results() {
entry:
  br i1 true, label %and.rhs, label %and.merge

and.rhs:                                          ; preds = %entry
  br label %and.merge

and.merge:                                        ; preds = %and.rhs, %entry
  %phi = phi i1 [ true, %entry ], [ true, %and.rhs ]
  br i1 %phi, label %and.rhs1, label %and.merge2

and.rhs1:                                         ; preds = %and.merge
  br label %and.merge2

and.merge2:                                       ; preds = %and.rhs1, %and.merge
  %phi11 = phi i1 [ %phi, %and.merge ], [ true, %and.rhs1 ]
  br i1 %phi11, label %and.rhs3, label %and.merge4

and.rhs3:                                         ; preds = %and.merge2
  br label %and.merge4

and.merge4:                                       ; preds = %and.rhs3, %and.merge2
  %phi12 = phi i1 [ %phi11, %and.merge2 ], [ true, %and.rhs3 ]
  br i1 %phi12, label %and.rhs5, label %and.merge6

and.rhs5:                                         ; preds = %and.merge4
  br label %and.merge6

and.merge6:                                       ; preds = %and.rhs5, %and.merge4
  %phi13 = phi i1 [ %phi12, %and.merge4 ], [ true, %and.rhs5 ]
  br i1 %phi13, label %and.rhs7, label %and.merge8

and.rhs7:                                         ; preds = %and.merge6
  br label %and.merge8

and.merge8:                                       ; preds = %and.rhs7, %and.merge6
  %phi14 = phi i1 [ %phi13, %and.merge6 ], [ true, %and.rhs7 ]
  br i1 %phi14, label %and.rhs9, label %and.merge10

and.rhs9:                                         ; preds = %and.merge8
  br label %and.merge10

and.merge10:                                      ; preds = %and.rhs9, %and.merge8
  %phi15 = phi i1 [ %phi14, %and.merge8 ], [ true, %and.rhs9 ]
  ret i1 %phi15
}
