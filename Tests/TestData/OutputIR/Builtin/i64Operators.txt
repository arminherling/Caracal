define main() i32
block entry(0):
    %0 = const true : bool
    %1 = const true : bool
    %2 = const true : bool
    %3 = const true : bool
    %4 = const true : bool
    %5 = const true : bool
    %6 = const true : bool
    branch if %0, and.rhs(1), and.merge(2)
block and.rhs(1):
    jump and.merge(2)
block and.merge(2):
    %7 = phi [entry(0): %0], [and.rhs(1): %1] : bool
    branch if %7, and.rhs(3), and.merge(4)
block and.rhs(3):
    jump and.merge(4)
block and.merge(4):
    %8 = phi [and.merge(2): %7], [and.rhs(3): %2] : bool
    branch if %8, and.rhs(5), and.merge(6)
block and.rhs(5):
    jump and.merge(6)
block and.merge(6):
    %9 = phi [and.merge(4): %8], [and.rhs(5): %3] : bool
    branch if %9, and.rhs(7), and.merge(8)
block and.rhs(7):
    jump and.merge(8)
block and.merge(8):
    %10 = phi [and.merge(6): %9], [and.rhs(7): %4] : bool
    branch if %10, and.rhs(9), and.merge(10)
block and.rhs(9):
    jump and.merge(10)
block and.merge(10):
    %11 = phi [and.merge(8): %10], [and.rhs(9): %5] : bool
    branch if %11, and.rhs(11), and.merge(12)
block and.rhs(11):
    jump and.merge(12)
block and.merge(12):
    %12 = phi [and.merge(10): %11], [and.rhs(11): %6] : bool
    branch if %12, if.true(13), if.continuation(14)
block if.true(13):
    %13 = const 1 : i32
    return %13
block if.continuation(14):
    %14 = const 0 : i32
    return %14
