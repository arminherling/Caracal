def main() i32
{
    a : [u8; _] = [...];
    b : [u8; _] = [...];
    s := string.new(a);
    t := string.new(b);
    if s.equals(t)
    {
        if t.notEquals(s)
        {
            return 1;
        }
        return 42;
    }
    return 2;
}
