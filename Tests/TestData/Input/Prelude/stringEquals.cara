def main() i32
{
    s := string.new([72, 101, ...]);
    t := string.new([72, 101, ...]);
    u := string.new([...]);
    if s.equals(t)
    {
        if s.notEquals(u)
        {
            return 42;
        }
        return 3;
    }
    return 2;
}
