def main() i32
{
    s := string.new([72, 101, ...]);
    t := string.new([72, 101, ...]);
    u := string.new([...]);
    if string.equals(s, t)
    {
        if string.notEquals(s, u)
        {
            return 42;
        }
        return 3;
    }
    return 2;
}
